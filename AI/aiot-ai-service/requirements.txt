# Re:Bloom LLM Server 의존성

# Web Framework
fastapi==0.116.1
uvicorn[standard]==0.35.0

# 데이터 검증 및 설정
pydantic==2.11.7
pydantic-settings==2.10.1

# 비동기 HTTP 클라이언트 (vLLM API 호출)
httpx==0.28.1

# SSE streaming (sse-starlette은 선택적 — FastAPI StreamingResponse로 대체 가능)
# sse-starlette==2.1.3

# Python-dotenv (pydantic-settings가 내부에서 사용)
python-dotenv==1.0.1
