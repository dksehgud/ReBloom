# 이 파일을 복사해서 .env.local로 사용합니다.
# 로컬 개발 기준:
# - PostgreSQL 인스턴스는 1개만 사용
# - 서비스별로 DB만 분리
SERVER_PORT=8081
DB_HOST=<shared-postgres-host>
DB_PORT=5432
DB_NAME=rebloom_auth
DB_USERNAME=rebloom
DB_PASSWORD=<database-password>
DB_SSL_MODE=disable
SPRING_PROFILES_ACTIVE=local
